'use strict';

const {
  EndpointConfig,
  SessionOptions,
  RandomConnectionIDStrategy,
  createEndpoint,
} = require('internal/quic/quic');

module.exports = {
  EndpointConfig,
  SessionOptions,
  RandomConnectionIDStrategy,
  createEndpoint,
};
